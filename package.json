{
  "name": "ayeza-cosmetics",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:api": "node scripts/railway-api-build.cjs",
    "build:web": "turbo run build --filter=@ayeza/web",
    "start": "turbo run start",
    "start:api": "node apps/api/dist/index.js",
    "railway:build": "node scripts/railway-api-build.cjs",
    "railway:start": "node apps/api/dist/index.js",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "prettier": "^3.4.2",
    "prettier-plugin-tailwindcss": "^0.6.9",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "packageManager": "npm@10.9.2"
}
